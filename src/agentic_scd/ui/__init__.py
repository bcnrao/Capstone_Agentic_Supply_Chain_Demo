"""Presentation layer (Phase 2 walking skeleton): a minimal Gradio dashboard.

The dashboard renders one end-to-end pipeline run across panels that map to the agent
stages (see specs/demo-walkthrough.md). Phase 8 promotes this skeleton to the full
dashboard and adds the parallel React UI over a FastAPI layer.
"""
