# API base path for the React UI. Use /api (default) so nginx or the Vite dev
# proxy forwards to FastAPI on the same host — works from any machine or cloud
# without rebuilding for each IP. Only set a full URL for split-host deploys.
VITE_API_BASE_URL=/api
